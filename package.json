{
  "name": "bezier-animator",
  "version": "1.0.5",
  "description": "A simple bezier curve callback animator",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start:debug": "node --inspect --require ts-node/register crappytest.ts",
    "start:debug:brk": "node --inspect-brk --require ts-node/register crappytest.ts"
  },
  "keywords": [
    "bezier",
    "curve",
    "animator"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/parkernilson/bezier-animator.git"
  },
  "author": "Parker Nilson",
  "license": "ISC",
  "devDependencies": {
    "@types/raf": "^3.4.0",
    "ts-node": "^8.1.0"
  },
  "dependencies": {
    "performance-now": "^2.1.0",
    "raf": "^3.4.1",
    "typescript": "^3.4.4"
  }
}
